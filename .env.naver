# 네이버 OAuth 설정
# 네이버 개발자 센터 (https://developers.naver.com) 에서 앱 생성 후 설정

# 네이버 앱 정보
NAVER_CLIENT_ID="your_naver_client_id"
NAVER_CLIENT_SECRET="your_naver_client_secret"

# 네이버 OAuth 리다이렉트 URI
# 네이버 개발자 센터 > 애플리케이션 > API 설정 > 네이버 아이디로 로그인 > Callback URL에 등록 필요
NAVER_REDIRECT_URI="http://localhost:3000/auth/naver/callback"

# 프로덕션 환경 설정 예시
# NAVER_REDIRECT_URI="https://yourdomain.com/auth/naver/callback"

# 네이버 API 권한 범위 (선택사항)
# 기본값: 네이버 아이디로 로그인
# 추가 가능: 이메일, 별명, 프로필 사진, 생일, 연령대, 성별